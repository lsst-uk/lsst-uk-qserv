Host ${jump}
    Hostname ${jump_ip}
    User ubuntu
    IdentityFile ~/.ssh/keys/${key_name}

%{ for index, node in nodes ~}
Host "${node}"
    Hostname ${node_ips[index]}
    proxyjump ${jump}
    User ubuntu
    IdentityFile ~/.ssh/keys/${key_name}
    proxyjump ${jump}
%{ endfor ~}
