Host ${jump}
    Hostname ${jump_ip}
    User ubuntu
    IdentityFile ~/.ssh/keys/somerville-openstack

%{ for index, node in nodes ~}
Host "${node}"
    Hostname ${node_ips[index]}
    proxyjump ${jump}
    User ubuntu
    IdentityFile ~/.ssh/keys/somerville-openstack
    proxyjump ${jump}
%{ endfor ~}
